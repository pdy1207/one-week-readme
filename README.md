# 프로젝트 개요

이 프로젝트는 마라톤 대회 참가 신청 시스템을 구축하는 웹 애플리케이션입니다.
참가자는 주어진 코스에서 선택하여 대회에 등록하며, 실시간 결제 정보 및 참가자 관리 기능을 포함합니다.

## 사용 버전

- **부트스트랩**: 5.3.8
- **부트스트랩 아이콘**: 1.11.1
- **jQuery**: 3.7.1
- **PHP**: 7.4.33
- **MySQL**: 5.6.51
- **WSL 사용**: Ubuntu-22.04
- **Nginx**: 1.29.7

## DB 구조

### 1:N 관계

- 참가자는 한 개의 대회에만 참여할 수 있으며, 검증은 휴대폰 번호로 진행됩니다.

### 테이블 구조

#### courses (코스) 테이블

| 컬럼 이름          | 타입         | 설명                         |
| ------------------ | ------------ | ---------------------------- |
| `id`               | INT UNSIGNED | PRIMARY KEY, 자동 증가       |
| `name`             | VARCHAR(50)  | 코스 이름 (예: 5km, 10km 등) |
| `description`      | VARCHAR(255) | 코스 설명                    |
| `price`            | INT(11)      | 참가비                       |
| `max_participants` | INT(11)      | 최대 참가 인원               |
| `created_at`       | TIMESTAMP    | 등록 시간                    |

#### registrations (참가자) 테이블

| 컬럼 이름          | 타입         | 설명                                     |
| ------------------ | ------------ | ---------------------------------------- |
| `id`               | INT(11)      | PRIMARY KEY, 자동 증가                   |
| `course_id`        | INT(11)      | 외래 키 (courses 테이블의 `id`)          |
| `name`             | VARCHAR(50)  | 참가자 이름                              |
| `birth`            | DATE         | 참가자 생년월일                          |
| `gender`           | CHAR(1)      | 참가자 성별                              |
| `phone`            | VARCHAR(20)  | 참가자 휴대폰 번호                       |
| `email`            | VARCHAR(100) | 참가자 이메일                            |
| `size`             | VARCHAR(5)   | 기념품 티셔츠 사이즈                     |
| `agree_rally`      | TINYINT(1)   | 대회 참가 동의 여부 (0: 미동의, 1: 동의) |
| `agree_info`       | TINYINT(1)   | 개인정보 동의 여부 (0: 미동의, 1: 동의)  |
| `agree_market`     | TINYINT(1)   | 마케팅 동의 여부 (0: 미동의, 1: 동의)    |
| `created_at`       | TIMESTAMP    | 등록 시간                                |
| `ip`               | VARCHAR(255) | 등록한 IP 주소                           |
| `zipcode`          | VARCHAR(255) | 우편번호                                 |
| `addr1`            | VARCHAR(255) | 주소                                     |
| `addr2`            | VARCHAR(255) | 상세 주소                                |
| `pay_complete`     | TINYINT(1)   | 결제 완료 여부 (0: 미결제, 1: 결제 완료) |
| `participant_code` | VARCHAR(20)  | 참가 번호                                |

## 대회 진행 흐름

### 메인 화면 (`index.php`)

- 코스별 정보를 제공하며, 참가자가 최대 인원에 도달하면 "마감" 표시가 나타납니다.
- 코스 설명에 따라 뱃지 색상이 달라집니다:
  - **가족 러닝**: `success` (그린)
  - **일반 코스**: `primary` (파랑)
  - **Half**: `warning` (노랑)
  - **Full**: `danger` (빨강)

### 대회 접수 절차

#### Step 1: 동의 화면 (`agree.php`)

- `agree.js`에서 동의 여부를 체크하여 값을 숨겨 전달합니다.
- 동의 여부를 `hidden` 필드를 통해 전달하며, 동의 값은 `0` (미동의) 또는 `1` (동의)로 구분됩니다.

#### Step 2: 코스 선택 (`cors.php`)

- `agree.js`에서 전달된 동의 값 검증 후, 코스 목록을 API로 불러와 바인딩합니다.
- 코스 카드를 선택하면 결제 예정 금액이 실시간으로 업데이트됩니다.

#### Step 3: 정보 입력 (`info.php`)

- 동의 값과 선택한 코스 값을 `queryString`으로 전달합니다.
- `sessionStorage`에 저장된 데이터가 있을 경우 복원하여 새로고침 시에도 데이터가 유지되도록 합니다.
- 코스 정보는 `readonly`로 설정되며, 배경색은 선택된 코스에 맞게 변경됩니다.
- `queryString` 값의 임의 조작 시, 세션이 삭제되고 대회 현황 페이지로 리다이렉트됩니다.
- 2008년 4월 7일 이후 출생자는 참가가 제한됩니다.

#### Step 4: 최종 제출 (`pay.php`)

- 모든 입력값에 대해 검증을 진행하고, 휴대폰 번호 중복 확인 후 결제 폼으로 넘어갑니다.

## 추가 사항

- **버전 관리**: 소스 코드와 관련된 버전은 오늘 날짜를 기준으로 표시됩니다.
- **코드 최적화 및 유지 관리**: 자주 사용하는 코드는 `header.php`와 `footer.php`에서 공통으로 관리되며, 레이아웃 부분은 `layout` 폴더에서 처리됩니다.

---

> 이 문서는 프로젝트의 개요와 사용법, 데이터베이스 구조 및 각 단계별 흐름에 대한 설명을 제공합니다.
